# Expo SDK 56 pulls a transitive dependency (vaul -> @radix-ui) that has not yet
# declared React 19 in its peer range. Use legacy peer resolution so installs
# stay consistent and don't prune required packages.
legacy-peer-deps=true
